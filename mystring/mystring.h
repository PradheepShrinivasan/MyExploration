// this is my own implementation of the string 
// done just for fun
#ifndef __MYSTRING_H__

#include <stdio.h>

extern char* mystrcpy (const char* src, char* dest);
extern int mystrlen (const char* src);
extern char* mystrncpy (const char* src, char* dest, int len);

#endif
